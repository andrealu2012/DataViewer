系统托盘图标替换说明
====================

tray.png：Windows 任务栏右侧通知区域（系统托盘）中显示的图标。
在 macOS 中，程序会自动把该图片作为系统“模板图标”处理，只使用透明轮廓，
并由系统根据菜单栏主题自动显示为黑色或白色。

打包后，用户可以直接替换发布目录中的 icons/tray.png，无需重新打包。
替换后请完全退出并重新启动软件。

建议使用带透明背景的正方形 PNG 图片，例如 32×32 或 64×64 像素。
本目录不用于修改应用程序或 exe 文件本身的图标。
